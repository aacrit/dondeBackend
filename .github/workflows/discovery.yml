name: Restaurant Discovery

on:
  schedule:
    # Enhancement 16: Run every 3 days instead of weekly for faster restaurant onboarding
    - cron: '0 3 * * 0,3'  # Sunday and Wednesday 3am UTC
  workflow_dispatch: {}    # Manual trigger

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci
        working-directory: scripts

      - name: Run discovery pipeline
        run: npx tsx pipelines/discovery.ts
        working-directory: scripts
        env:
          SUPAB_URL: ${{ secrets.SUPAB_URL }}
          SUPAB_SERVICE_ROLE_KEY: ${{ secrets.SUPAB_SERVICE_ROLE_KEY }}
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
